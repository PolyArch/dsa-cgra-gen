package cgra.parameter

import scala.collection.mutable.Map

object new_config extends App {
/*
  val MapParam = Map[IsKey,List[IsParameters]]()

  var mux_param : List[Mux_param] = Nil
  var delay_pipe_param : List[Delay_pipe_delay_param] = Nil
  var alu_param : List[Alu_inst_param] = Nil

  val test_index = new port_subnet_operand("",0,1,2)

  alu_param = alu_param ::: List(get_alu_param(2))
  alu_param = alu_param ::: List(get_alu_param(1))
  alu_param = alu_param ::: List(get_alu_param(0))

  MapParam += test_index -> alu_param
  if(MapParam.isDefinedAt(test_index))
    MapParam += test_index -> (MapParam(test_index) ::: List(get_alu_param(2)))
  else
    MapParam += test_index -> List(get_alu_param(2))

  def filter (ps:IsKey, msp: List[IsParameters]) = true

  val te = MapParam.filter(x=>filter(x._1,x._2))

  def get_alu_param(inst:Int) : Alu_inst_param = {
    new Alu_inst_param(inst)
  }
*/
  println("Hello World")
}