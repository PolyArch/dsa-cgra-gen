package cgra.fabric

import cgra.IR.router
import chisel3._
import chisel3.util._
import scala.collection.immutable.HashMap

class Router_Hw(r:router) extends Module {
  val io = IO(new Bundle{

  })
}